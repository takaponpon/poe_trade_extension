name: Build and Release Extension

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build extension
        run: npm run build

      - name: Get version from manifest.json
        id: get_version
        run: |
          VERSION=$(jq -r '.version' manifest.json)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release zip
        run: |
          zip -r poe_trade_extension_v${{ steps.get_version.outputs.VERSION }}.zip manifest.json background.js dist/content.js content.css poe_ninja_content.js poe_ninja_content.css pako.min.js items_translation_map.json stats_translation_map.json popup.html popup.js popup.css icons/

      - name: Update or Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "poe_trade_extension_v${{ steps.get_version.outputs.VERSION }}.zip"
          tag: "v${{ steps.get_version.outputs.VERSION }}"
          name: "Release v${{ steps.get_version.outputs.VERSION }}"
          generateReleaseNotes: true
          allowUpdates: true
          token: ${{ github.token }}
